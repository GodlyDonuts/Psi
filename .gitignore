# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.env

# Native build artifacts (C / CUDA / Metal kernels)
*.o
*.obj
*.so
*.dylib
*.dll
*.a
*.metallib
build/
dist/
cmake-build-*/
# compiled demo executables (built into the repo root by the README one-liners)
/step0
/step1
/step2_gradcheck
/psi_nano
/matmul_bench
/psi_profile
/matmul_metal
/bridge_test
/agtest
/attention_metal

# ML artifacts — never commit weights or raw datasets (but DO commit per-model text records:
# each models/<name>/ keeps MODEL.md + train.txt + eval.txt so a model is reproducible).
*.bin
*.pt
*.safetensors
*.gguf
checkpoints/
data/*.txt
data/*.bin
datasets/
runs/
wandb/
*.log

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp
/shape_bench
/db_dev
/mma_autotune
/ternary_gemm
/ternary_fp16
/fused_ternary
/bpe_test
/psi_stories
/results/
